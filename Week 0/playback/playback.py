# Ask user for the message
message = input("Input the message: ")

# Change whitespace to three dots (...)
new_message = message.replace(" ", "...")

# Print the message with three dots replacing whitespaces
print(new_message)
